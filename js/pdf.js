App.Pdf={header(title){return '<div class="doch"><img src="'+App.logoPath+'" alt=""><div><h2>'+App.esc(App.db.settings.name)+'</h2><p>'+App.esc(App.db.settings.address)+'</p><b>'+App.esc(title)+'</b></div></div>'},admissionHtml(a){return this.header("Admission Form")+'<div class="docgrid"><div><b>Date:</b> '+App.esc(a.date)+'</div><div><b>Admission ID:</b> '+App.esc(a.admissionId)+'</div><div><b>Student:</b> '+App.esc(a.studentName)+'</div><div><b>Class:</b> '+App.esc(a.className)+'</div><div><b>Father:</b> '+App.esc(a.fatherName)+'</div><div><b>Mother:</b> '+App.esc(a.motherName)+'</div><div><b>Mobile:</b> '+App.esc(a.mobile)+'</div><div><b>School:</b> '+App.esc(a.schoolName||"-")+'</div><div><b>Batch:</b> '+App.esc(a.batch||"-")+'</div><div><b>Address:</b> '+App.esc(a.address||"-")+'</div></div><div class="signature-row"><div>Parent Signature</div><div>Authorized Signature</div></div>'},receiptHtml(f){return this.header("Fees Receipt")+'<div class="docgrid"><div><b>Receipt No.:</b> '+App.esc(f.receiptNo)+'</div><div><b>Date:</b> '+App.esc(f.date)+'</div><div><b>Student:</b> '+App.esc(f.studentName)+'</div><div><b>Class:</b> '+App.esc(f.className||"-")+'</div><div><b>Month:</b> '+App.esc(f.month||App.monthKey(f.date))+'</div><div><b>Payment Mode:</b> '+App.esc(f.paymentMode||"Cash")+'</div><div><b>Monthly Fees:</b> '+App.rs(f.monthly)+'</div><div><b>Paid Fees:</b> '+App.rs(f.fees)+'</div><div><b>Total Paid:</b> '+App.rs(f.total)+'</div><div><b>Transaction ID:</b> '+App.esc(f.transactionId||"-")+'</div></div><div class="seal">Rainbow<br>Seal</div><div class="signature-row"><div>Received By</div><div>Authorized Signature</div></div>'},async blob(title,html){const wrap=document.createElement("div");wrap.className="pdf-page";wrap.innerHTML='<section class="doc ready">'+html+'</section>';document.body.appendChild(wrap);try{if(!window.html2pdf)return null;return await window.html2pdf().set({margin:10,filename:App.fileName(title)+".pdf",image:{type:"jpeg",quality:.98},html2canvas:{scale:2,useCORS:true},jsPDF:{unit:"mm",format:"a4",orientation:"portrait"}}).from(wrap).outputPdf("blob")}finally{wrap.remove()}},openPrint(title,html){const w=window.open("","_blank","width=900,height=1100");if(!w){App.toast("Popup blocked");return}w.document.write('<!doctype html><html><head><title>'+App.esc(title)+'</title><link rel="stylesheet" href="styles.css"><style>@page{size:A4;margin:10mm}body{background:#fff}.doc{display:block!important;border:0;box-shadow:none}</style></head><body><section class="doc ready">'+html+'</section></body></html>');w.document.close();setTimeout(()=>{w.focus();w.print()},350)},async ensure(type,item,title,html,file){if(item.pdfUrl)return item.pdfUrl;const b=await this.blob(title,html);if(!b){this.openPrint(title,html);return ""}try{const url=await App.Storage.upload(type,item,b,file);if(url)App.toast("PDF Supabase Storage me save ho gayi");return url}catch(e){console.warn(e);App.toast("PDF Storage issue: documents bucket/policy check karein");return ""}},async download(type,item,title,html,file){await this.ensure(type,item,title,html,file);const b=await this.blob(title,html);if(!b)return this.openPrint(title,html);const u=URL.createObjectURL(b),a=document.createElement("a");a.href=u;a.download=file;a.click();URL.revokeObjectURL(u)},async preview(type,item,title,html,file){const u=await this.ensure(type,item,title,html,file);u?window.open(u,"_blank"):this.openPrint(title,html)},async share(type,item,title,html,file){const url=await this.ensure(type,item,title,html,file);const b=await this.blob(title,html);if(b&&navigator.share){const f=new File([b],file,{type:"application/pdf"});if(!navigator.canShare||navigator.canShare({files:[f]})){try{await navigator.share({title,text:title+" - "+(item.studentName||"")+(url?"\n"+url:""),files:[f]});return}catch(e){if(e.name==="AbortError")return}}}if(url&&navigator.share){try{await navigator.share({title,text:title,url});return}catch{}}url?window.open(url,"_blank"):App.toast("Share support available nahi hai")},actions(target,item,type,html){const title=type==="admission"?"Admission Form":"Fees Receipt";const file=App.fileName((type==="admission"?item.admissionId:item.receiptNo)+"-"+item.studentName)+".pdf";App.$(target).insertAdjacentHTML("beforeend",'<div class="actions"><button class="blue" id="'+type+'SavePdf">Save PDF</button><button class="ghost" id="'+type+'Preview">Preview</button><button class="green" id="'+type+'Share">Share</button><button id="'+type+'Wa">WhatsApp</button></div>');App.$('#'+type+'SavePdf').onclick=()=>this.download(type,item,title,html,file);App.$('#'+type+'Preview').onclick=()=>this.preview(type,item,title,html,file);App.$('#'+type+'Share').onclick=()=>this.share(type,item,title,html,file);App.$('#'+type+'Wa').onclick=()=>App.whatsapp(item.mobile,type==="admission"?"Admission Form: "+item.studentName+", ID "+item.admissionId:"Receipt "+item.receiptNo+": "+item.studentName+" ki fees "+App.rs(item.total)+" receive ho gayi. - Rainbow The Learner Zone")},showAdmission(a){const h=this.admissionHtml(a);App.$("#admissionDoc").className="doc ready";App.$("#admissionDoc").innerHTML=h;this.actions("#admissionDoc",a,"admission",h)},showReceipt(f){const h=this.receiptHtml(f);App.$("#receiptDoc").className="doc ready";App.$("#receiptDoc").innerHTML=h;this.actions("#receiptDoc",f,"fees",h)}};