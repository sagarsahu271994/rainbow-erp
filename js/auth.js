App.Auth={apply(){const ok=sessionStorage.getItem(App.AUTH_KEY)==="yes";document.body.classList.toggle("locked",!ok);const s=App.$("#loginScreen");if(s)s.style.display=ok?"none":"grid"},async login(e){e.preventDefault();const x=App.formData(e.target);const enteredHash=App.sha256?await App.sha256(x.password):"";if(x.username!==App.AUTH_USER||enteredHash!==App.AUTH_PASS_HASH){App.$("#loginError").classList.add("show");return}sessionStorage.setItem(App.AUTH_KEY,"yes");App.$("#loginError").classList.remove("show");App.Auth.apply();App.toast("Login successful")},logout(){sessionStorage.removeItem(App.AUTH_KEY);App.Auth.apply();App.toast("Logged out")}};
